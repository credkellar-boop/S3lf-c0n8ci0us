class BiometricBridge:
    def scan(self, scan_type):
        """Types: 'face', 'fingerprint', 'retina'"""
        data = self._poll_hardware(scan_type)
        return hash(data)

    def _poll_hardware(self, scan_type):
        # Interface with system-level security APIs
        return f"raw_{scan_type}_signal"
